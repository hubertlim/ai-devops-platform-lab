apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: ai-platform
  labels:
    app: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: api
